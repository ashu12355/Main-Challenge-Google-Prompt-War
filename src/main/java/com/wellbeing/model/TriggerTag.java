package com.wellbeing.model;

/**
 * Triggers for student stress levels under Indian-context pressures.
 */
public enum TriggerTag {
    /** Coaching class backlog. */
    COACHING_CLASS_BACKLOG,
    /** Mock test percentile stress. */
    MOCK_TEST_PERCENTILE,
    /** Peer comparison stress. */
    PEER_COMPARISON,
    /** Family expectations stress. */
    FAMILY_EXPECTATIONS,
    /** Homesickness and hostel food. */
    HOMESICKNESS,
    /** General Mock Test stress. */
    MOCK_TEST,
    /** Syllabus completion pressure. */
    SYLLABUS,
    /** Parent and relative pressure. */
    PARENT_PRESSURE
}
